# Roads-and-Libraries-ALGO-MEDIUM.This solution finds the minimum cost to provide library access to all cities by checking if it's cheaper to build libraries everywhere versus building roads to connect cities into components where each component needs only one library. It uses BFS to find connected city components and calculates the optimal cost based on library and road prices.

